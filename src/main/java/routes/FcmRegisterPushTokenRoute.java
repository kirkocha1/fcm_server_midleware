package routes;

import spark.Request;
import spark.Response;
import spark.Route;

public class FcmRegisterPushTokenRoute implements Route {

    public static String PATH = "registerToken";

    @Override
    public Object handle(Request request, Response response) throws Exception {
        return null;
    }
}
